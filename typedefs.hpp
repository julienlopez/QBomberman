#ifndef TYPEDEFS_HPP
#define TYPEDEFS_HPP

#include <cstdint>

namespace QBomberMan {

typedef uint16_t type_key;

using EntityId = uint16_t;

enum KEYS {NONE, UP, DOWN, LEFT, RIGHT, SPACE};

}

#endif // TYPEDEFS_HPP
